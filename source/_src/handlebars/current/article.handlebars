{{#each this as |articles articleSection|}}
    {{#is articleSection 'uir'}}
        {{> article-publish-modal}}
    {{/is}}
    {{# if articles.length}}
        <section class="{{articleSection}}-list" id="{{articleSection}}-list">
            {{#is articleSection 'uir'}}
                <button href="#" class="btn btn-default publish-all btn-publish-queued" data-toggle="modal"
                        data-target="#publish-modal" type="button">
                    <span class="glyphicon glyphicon-globe"></span>
                    Publish All Selected
                </button>
            {{/is}}
            <table class="article-snapshot-list">
                {{#is articleSection 'error'}}
                    <caption>
                        <span class="glyphicon e-icon lg danger glyphicon-warning-sign  pull-left"></span>
                        <h4>Errors
                            {{# if articles.length}}
                                <small>({{articles.length}} Articles)</small>
                            {{/if}}
                        </h4>
                    </caption>
                {{/is}}
                {{#is articleSection 'inProgress'}}
                    <caption>
                        <span class="glyphicon e-icon lg info glyphicon-cog  pull-left"></span>
                        <h4>In Progress
                            <small>({{articles.length}} Articles)</small>
                        </h4>
                    </caption>
                {{/is}}
                {{#is articleSection 'uir'}}
                    <caption>
                        <span class="glyphicon e-icon lg muted glyphicon-hand-down  pull-left"></span>
                        <h4>
                            User Input Required
                            {{# if articles.length}}
                                <small>({{articles.length}} Articles)</small>
                            {{/if}}
                            <br>(Ready to Publish)
                        </h4>
                    </caption>
                {{/is}}
                {{#is articleSection 'scheduled'}}
                    <caption>
                        <span class="glyphicon e-icon lg warning glyphicon-time  pull-left"></span>
                        <h4>Scheduled
                            {{# if articles.length}}
                                <small>({{articles.length}} Articles)</small>
                            {{/if}}
                        </h4>
                    </caption>
                {{/is}}
                <tbody>
                {{#each articles}}
                    {{> article-item section=articleSection }}
                {{/each}}
                </tbody>
            </table>
        </section>
    {{/if}}
{{/each}}