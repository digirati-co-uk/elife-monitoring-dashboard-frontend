<section class="article-version-history">
    <ol class="article-version-history-list">
        {{#each currentEvents.events as |events|}}
            <li>
                <div class="column-1 {{event-status}}">
                    {{#is event-status 'start'}}
                        <span class="fa e-icon lg warning fa-exclamation-triangle  pull-left" title="{{event-status}}"></span>
                    {{/is}}
                    {{#is event-status 'end'}}
                        <span class="fa e-icon lg success fa-check  pull-left" title="{{event-status}}"></span>
                    {{/is}}
                    {{#is event-status 'error'}}
                        <span class="fa e-icon lg danger fa-times  pull-left" title="{{event-status}}"></span>
                    {{/is}}
                    <span>{{event-type}}</span>
                </div>
                <div class="column-2">
                    <dl>
                        <dt><i>Timestamp:</i></dt>
                        <dd class="divide"><strong>{{elFormatUnixDate event-timestamp "DD/MM/YYYY" }}</strong></dd>
                        <dd class="divide"><strong>{{elFormatUnixDate event-timestamp "HH:mm:ss" }}</strong></dd>
                    </dl>
                    <dl>
                        <dt><i>Message:</i></dt>
                        <dd>{{event-message}}</dd>
                    </dl>
                </div>
            </li>
        {{/each}}
    </ol>
</section>